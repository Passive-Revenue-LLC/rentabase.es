---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ScrollToTop from '../components/ScrollToTop.astro';
import {
  generateSEO,
  generateWebsiteJsonLd,
  generateOrganizationJsonLd,
  type SEOProps,
} from '../utils/seo';
import '../styles/article.css';

interface Props {
  seo: SEOProps;
  /** JSON-LD adicional para la página */
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  /** Excluye la página de la indexación (ej. 404) sin bloquear el rastreo de sus enlaces */
  noindex?: boolean;
}

const { seo, jsonLd, noindex = false } = Astro.props;
const jsonLdItems = jsonLd ? (Array.isArray(jsonLd) ? jsonLd : [jsonLd]) : [];
const meta = generateSEO(seo);
const websiteJsonLd = generateWebsiteJsonLd();
const organizationJsonLd = generateOrganizationJsonLd();
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO básico -->
    <title>{meta.title}</title>
    <meta name="description" content={meta.description} />
    <link rel="canonical" href={meta.canonical} />
    <meta
      name="robots"
      content={noindex
        ? 'noindex, follow'
        : 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'}
    />

    <!-- hreflang -->
    <link rel="alternate" hreflang="es-ES" href={meta.canonical} />
    <link rel="alternate" hreflang="x-default" href={meta.canonical} />

    <!-- Open Graph -->
    <meta property="og:title" content={meta.openGraph.title} />
    <meta property="og:description" content={meta.openGraph.description} />
    <meta property="og:image" content={meta.openGraph.image} />
    <meta property="og:url" content={meta.openGraph.url} />
    <meta property="og:type" content={meta.openGraph.type} />
    <meta property="og:site_name" content="RentaBase" />
    <meta property="og:locale" content="es_ES" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content={meta.twitter.card} />
    <meta name="twitter:site" content={meta.twitter.site} />
    <meta name="twitter:creator" content={meta.twitter.creator} />
    <meta name="twitter:title" content={meta.twitter.title} />
    <meta name="twitter:description" content={meta.twitter.description} />
    <meta name="twitter:image" content={meta.twitter.image} />
    <meta name="twitter:image:alt" content={meta.twitter.imageAlt} />

    <!-- JSON-LD: WebSite + Organization -->
    <script type="application/ld+json" set:html={JSON.stringify(websiteJsonLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(organizationJsonLd)} />

    <!-- JSON-LD adicional (artículo, breadcrumbs, etc.) -->
    {jsonLdItems.map((item) => (
      <script type="application/ld+json" set:html={JSON.stringify(item)} />
    ))}

    <!-- Favicon & Manifest -->
    <link rel="icon" href="/favicon.ico" />
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#283F3B" />

    <!-- RSS Feed -->
    <link rel="alternate" type="application/rss+xml" title="RentaBase" href="/rss.xml" />

    <!-- Tipografía autohospedada (antes Google Fonts CDN: una petición externa
         render-adyacente menos, sin depender de fonts.gstatic.com) -->
    <style is:global>
      @font-face {
        font-family: 'Funnel Display';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url('/fonts/FunnelDisplay-Regular.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Funnel Display';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url('/fonts/FunnelDisplay-Medium.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Funnel Display';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url('/fonts/FunnelDisplay-SemiBold.woff2') format('woff2');
      }
      @font-face {
        font-family: 'Funnel Display';
        font-style: normal;
        font-weight: 700;
        font-display: swap;
        src: url('/fonts/FunnelDisplay-Bold.woff2') format('woff2');
      }
    </style>

    <!-- Google Consent Mode v2: denegado por defecto hasta que el usuario decida.
         Debe ejecutarse antes que la CMP, gtag.js y AdSense. -->
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('consent', 'default', {
        'ad_storage': 'denied',
        'ad_user_data': 'denied',
        'ad_personalization': 'denied',
        'analytics_storage': 'denied',
        'wait_for_update': 500
      });
    </script>

    <!-- TODO(CMP): pegar aquí el script de CookieYes en cuanto exista la cuenta.
         Debe ir antes de gtag.js y de AdSense para poder actualizar el consentimiento
         (gtag('consent', 'update', ...)) antes de que esos tags carguen recursos.
         <script id="cookieyes" type="text/javascript" src="https://cdn-cookieyes.com/client_data/TU_ID/script.js"></script> -->

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-H6C37NP4RL"></script>
    <script is:inline>
      gtag('js', new Date());
      gtag('config', 'G-H6C37NP4RL');
    </script>

    <!-- Google AdSense -->
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8726052197962848" crossorigin="anonymous"></script>
  </head>
  <body class="min-h-screen flex flex-col bg-fondo text-texto font-body antialiased">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
    <ScrollToTop />

    <!-- Animaciones de entrada al hacer scroll (IntersectionObserver) -->
    <style>
      .reveal {
        opacity: 0;
        transform: translateY(24px);
        transition: opacity 0.7s cubic-bezier(0.22, 1, 0.36, 1),
                    transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
      }
      .reveal.is-visible {
        opacity: 1;
        transform: translateY(0);
      }
      /* Stagger para grids de tarjetas */
      .reveal-stagger > .reveal:nth-child(2) { transition-delay: 0.1s; }
      .reveal-stagger > .reveal:nth-child(3) { transition-delay: 0.2s; }
      .reveal-stagger > .reveal:nth-child(4) { transition-delay: 0.15s; }
      .reveal-stagger > .reveal:nth-child(5) { transition-delay: 0.25s; }
      .reveal-stagger > .reveal:nth-child(6) { transition-delay: 0.3s; }
    </style>

    <script>
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.08, rootMargin: '0px 0px -40px 0px' }
      );

      document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
    </script>
  </body>
</html>
